# 17 · 顾问工作台与沟通表单

P0，待做；前置16。替换pages/Overview.tsx目前只读预览，新增FollowUps.tsx、FollowUpDialog.tsx；/、/follow-ups仅Admin。

工作台真实指标：我的开放待办、逾期、今日试听、结果待补录；四个计数来自 GET /dashboard（本任务同步实现Admin聚合API，严格负责人scope），默认展示最近到期待办，点击一跳打开沟通表单。教师仍进课表，不能获取机构或销售指标。

待办列学生、试听时间/科目/老师、结果摘要、到期、状态；筛选开放/逾期/关闭，服务端分页。详情上半区反馈与历史，下半区实际联系人、渠道、沟通时间、结果、内容、下一步时间/关闭原因。下一步时间用Melbourne，不使用浏览器默认时区。默认不预选“已联系成功”。

保存按钮为Save communication；请求携带expectedVersion+稳定幂等键，同一次失败重试复用键，用户改正文后换键；409提示重新加载记录并保留编辑内容，不自动重提交覆盖。成功后刷新工作台/待办/学生时间线。

验收：老师提交反馈后Admin出现该学生待办；未联系上保留开放并改未来时间；暂不考虑明确原因关闭；Interested仍开放；逾期随时间自然变化；不能只改UI为完成。全流程刷新持久化，失败保留输入。
