# 04 · 学生列表、录入与详情

P0；基础列表/录入已做，详情/编辑待做。前置03；联系人区依赖05，咨询区依赖07。

## 文件 / 页面

扩 pages/Students.tsx，新 pages/StudentDetail.tsx、hooks/useStudents.ts；路由 /students、/students/:id。Admin Students，Teacher My students。列表姓名可点击，搜索分页由服务器处理，创建仅Admin可见；基础表单姓名+年级，默认Not assessed。

详情顶部姓名/年级/负责人（Admin）/归档状态；按授权DTO显示概况、教学记录；负责人可见家长、咨询、试听、跟进时间线。未实现子模块不造假数据或可点击空按钮；随着相应任务完成再接入。教师页面不显示联系方式、意向、订单标签。

## 交互

编辑弹窗带expectedVersion；409提示重新加载但保留用户输入，不能自动覆盖；归档前展示关联阻断原因。保存后失效学生列表+详情，新增时回第一页清搜索，并显示成功提示。非负责人看到基础页和只读状态，不显示不可用销售表单。

## 验收

录入真实学生刷新仍在；重名可建；输入只空格不可提交；401回登录；500保留表单可重试。教师侧无Add/Edit按钮且直达其他学生详情无数据。窄屏列表可滚动，表单键盘提交和关闭正确。
