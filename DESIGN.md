# 学生管理系统：试听预约与课后跟进

**开发数据约定（2026-09-17）：**当前未发布，用户已明确不需要历史数据兼容，本项目开发库可直接删除重建并重新seed。后续移除旧字段和流程时，不增加历史回填、双版本或旧收据隔离；新业务自身的流水、审计和幂等仍保留。下文旧阶段验收只是当时事实。

## 1. 业务理解与本次切片

顾问最需要知道“学生下一步该约课还是联系”，老师最需要知道“今天谁来、谁要重点观察”。因此围绕一次试听连接排课、教学反馈和顾问行动，而不是把每张表都做成管理页面。仅Admin和Teacher登录；家长是沟通对象，学生是学习对象，二者信息分别保存。

**本次Part B：学生建档 → 加入课程 → 老师签到 → 下课后逐试听学生评价 → 负责Admin的待办 → AI辅助 → 记录沟通结果并完成本次跟进。** 选择它，是因为它同时覆盖排课冲突、老师识别试听生和试听后漏跟进三个痛点，能在6–10小时预算内验证跨角色、跨前后端的完整流程。

本次交付终点是有据可查的跟进结果，未报名也可以完成任务。已有课时发放继续复用，不扩展支付或套餐管理。以下开发记录说明历史实现，最新签到/个人评价与精简规则尚未完整接入；真实LLM成功调用和最终回归仍待验证。2026-09-17另行完成权益发放后端及学生/课时页面：可选赠课、会员三Tab、余额和流水、三种发放均可在隔离库演示，见[后端验收](docs/ENTITLEMENT-MILESTONE.md)与[页面验收](docs/ENTITLEMENT-UI-MILESTONE.md)。新增预约已接入双池占用校验及明确重约来源；学生身份已独立为type，预约资格统一读取课时卡；取消单个预约已完成占用释放与本来源重约待办；恢复预约已重新校验资格并只关闭本来源。同科目换课已完成原子占用转移、目标取消行恢复、明确待办关联及双向变更记录；04d名单、课表人数与教学记录已统一会员分类，05a名单选人及取消/恢复/换课界面已接入双池与明确重约来源；课后扣减与其他待办消费者仍待完成，不把局部权益能力与旧试听规则混成已发布版本。账号管理按本轮用户追加要求已实现，见[开发计划](.plan/README.md)。

**05c已完成：**名单仅保留加入与移除；取消记录只读，重新预约通过普通Add，跨课程调整分两步执行。删除Move/Restore、目标课程选择与重约来源锁定/透传，旧来源URL参数自动清理。联系人不完整不拦预约，双池余额及不足补课返回继续保留；本人学生、重复加入、提交中和课时校验仍有效。Chrome与数据库核对通过，类型检查、9项单元测试及构建通过，见[名单页面验收](docs/BOOKING-UI-SIMPLIFICATION-MILESTONE.md)。旧公开接口由09d清退，下一项06b签到命令。

**04f已完成：**名单加入/移除不再生成重约；已取消且未消费的参与行可通过Add重新加入并重新选卡。只保留学生时间冲突、权限、状态、同课唯一和对应卡可用量；加入试听身份学生预建一条隐藏评价，取消关闭、重加复用，会员用试听卡不产生试听任务。完整新版签到/待办筛选由后续任务接入，名单界面已由05c清理，见[预约简化验收](docs/BOOKING-SIMPLIFICATION-MILESTONE.md)。

**03c已完成：**课程创建/编辑仅保留班级、科目、老师与起止时间，带学生名单也可修改；取消为独立原因弹窗。课表只显示实际人数，409保留修改草稿并刷新版本，已开始/取消/有结果课程只读，Teacher无管理入口。Chrome、类型检查、9项单元测试及构建通过，见[页面验收](docs/SESSION-EDITOR-MILESTONE.md)。预约后端旧容量/联系人门槛已由04f移除；此处不代表新签到/个人评价闭环已完成。

**03b已完成：**取消未来课程会同事务取消全部有效预约和未完成的逐学生教师评价任务，释放占用；不会增加课时流水或生成重约待办。接口只需版本和原因，重复/并发与失败回滚已验证。页面已按03c收敛，见[取消验收](docs/SESSION-CANCEL-MILESTONE.md)。

**03a已完成：**课程创建/修改的排课限制只检查老师和全体有效学生的时间冲突；有名单也可改科目、老师和时间，修改同事务更新逐学生待评价任务。空课程不再生成整课待办。OpenAPI与后端验证已完成，见[课程命令验收](docs/SESSION-COMMAND-MILESTONE.md)。页面调整已由03c完成，预约容量等门槛已由04f移除，取消规则已按03b收敛；当前仍是开发中间状态。

**最新进度（01g）：**签到时间/操作者、个人评价提交时间、逐学生教师任务类型、跟进最终结果及顾虑/核心问题的数据契约已完成；沟通API已支持新增字段，OpenAPI已同步。增量迁移在隔离PG验证，旧数据不伪造新时间或结果；真实签到、隐藏待办生成/筛选、个人评价及新完成态命令仍由后续任务实现，见[01g验收](docs/CHECKIN-CONTRACT-MILESTONE.md)。

## 2. 范围与取舍

| 本次做 | 为什么 |
| --- | --- |
| 学生及主要联系人、咨询备注、沟通记录 | 同一处保存咨询和跟进依据，避免另建CRM |
| 全班课表、单次排课、名单调整与变更记录 | 顾问能直接寻找空位、避免冲突，老师看到实际名单 |
| 试听优先标签、出勤反馈、两类个人待办 | 让教学观察交到负责顾问手中，明确下一步责任人 |
| 跟进详情中的AI建议 | 减少阅读反馈及组织沟通提纲的时间；失败仍能人工处理 |

**有意后做：**已完成课时发放和套餐选择继续复用，但套餐管理、动态优惠和续费提醒不再扩展；固定周课及长期入班会增加周期和调课例外，先验证单次预约；支付、退款涉及收款核对及审批，本次只设计追加流水的扩展方向。家长资料内嵌Student，不建独立家长管理，接受兄弟姐妹重复录入以减少操作；动态角色/菜单配置、家长端与自动发消息不影响试听闭环，暂不做。班级、科目仍以seed维护；员工管理已接入super账号模块。旧数据核对导入及生产切换演练延期。专用换课/恢复/重约流程退出本期；单个学生换课由Admin先移除再添加，不承诺跨两步自动回滚。

## 3. 模型、关系与课时

| 实体 | 关键字段与关系 |
| --- | --- |
| User / Student / StudentAdminLink | 用户角色；学生name、yearLevel、type（TRIAL/MEMBER）及主要联系人。关联表保存studentId、当前adminId、最初createdByAdminId与创建时间；一生一个当前负责人，一个Admin可负责多名学生。联系方式不唯一 |
| ClassGroup / Course / ClassSession | 长期班级 / 科目 / 单次课。课次关联班级、科目、老师、起止时间和状态；班级仅分组，旧容量字段不再作为本期限制 |
| SessionParticipant | sessionId+studentId唯一；kind仅记录所用课时卡TRIAL/REGULAR；记录预约状态、出勤、checkedInAt/checkedInBy、个人反馈及feedbackSubmittedAt，不表示课程或学生类型；学生可参加不同班级的课次 |
| Task / CommunicationLog | 老师每个试听参与记录一个TRIAL_FEEDBACK；Admin每个已评价且仍为试听身份的参与记录一个跟进任务。Task.status和followupOutcome分开；沟通只追加，保存家长反馈、concerns、coreQuestion、reasonTags |
| ScheduleChange | 排课或名单操作的原因、操作者、时间、前后值，与业务修改同时保存 |
| 后续：EntitlementEntry / LessonPackage | 不可变课时流水 / 预设正式课时套餐；兼容模型及发放/查询后端已建，用户旧库未核对导入，不作为旧切片余额凭证 |

**学生基本资料：**独立保存学生性别gender与年龄age，不混用家长字段。新建/编辑均可录入，年龄为0–120整数并允许清空，未知性别可留空；不从姓名、年级推算。年龄表示人工登记值，不按年份自动增长。学生列表显示姓名、年级、学生性别、学生年龄；Admin额外看到当前负责Admin姓名（取StudentAdminLink），不是原始录入人。Teacher只看到本人教学范围学生的基本信息，不增加负责人/录入人或家长资料访问权限。

**建档与家长资料：**新增学生时可直接录入家长姓名、与学生关系、工作、年龄、性别、邮箱、手机/电话、微信、沟通方式及语言，同时填写意向科目、时间和学习目标；编辑与详情保持相同字段。尚未问清的信息允许留空，年龄为可清空的0–120整数、性别可不提供，不推断家庭属性；邮箱/电话有值时必须合法；联系人不完整暂不阻止预约，由Admin补齐以便沟通。家长资料内嵌学生，兄弟姐妹可重复，不做联系人去重或独立家长管理页。这些字段仅本人负责Admin可见，不进入Teacher响应，也不因增加字段而自动发送给AI。

**学生与Admin关联：**后端从登录会话确定录入人，不接受客户端传入负责人、录入人或嵌套关联。建档、StudentAdminLink、赠课和收据同事务；新记录的当前负责人和录入人都为当前Admin。首次录入人一经记录不可修改，后续交接仅改变负责人；Admin详情显示双方姓名，Teacher不返回员工关联字段。历史记录只回填现有负责人，录入人未知保留NULL。当前兼容阶段的唯一归属写入口仍是Student.ownerAdminId，数据库触发器同步关联表，并通过唯一/外键约束保证每生恰好一条匹配关联；不能直接单独修改关联表造成权限不一致。待整体切换时再退役兼容字段，不在本次同时重写全部预约/跟进消费者。

**课表是ClassSession的日历/列表查询，没有课表实体。所有课次都是正常课程，不分“试听课程”和“正式课程”。** 同一课次可以有试听生、新会员及会员；预约kind只指定使用的课时卡，不能用于判断学生身份。

**学生身份：**Student.type是身份依据，枚举TRIAL（试听学生）与MEMBER（正式会员）。所有新建学生默认TRIAL，包括未勾选赠卡的学生。首次录入任何非试听课时卡（自定义正式卡或会员套餐）时，在发卡同一事务内将type设为MEMBER并记录firstPurchasedAt。后续发试听卡、使用剩余试听课时、续购或余额归零，都不改变会员身份。资料编辑接口不能直接修改type或首次购买时间。三Tab：type=TRIAL进入试听学生；type=MEMBER再按首次购买的墨尔本日期分为7日内新会员、之后会员学生。“新会员”是会员的时间分类，不是第三个持久化type。

**课时卡与流水（建档、分类和发放页面已实现，消费链路仍待完成）：**新建学生默认勾选赠送试听卡，选中则在现有课时表EntitlementEntry创建TRIAL/INITIAL_TRIAL、quantity=1的记录；未选中不写记录。课时管理追加试听卡节数，或录入正式卡节数/选择正式会员套餐；不另建重复的卡余额表。两类卡跨科目通用、不可互抵，预约明确选择所用卡且每次占1节。新设计在老师签到时扣1，评价不再扣课；取消或未签到到下课后不再占用。占用查询限定未签到且endsAt大于同次服务端now，课后补签须重新校验余额；06b负责实现。会员也可以用试听卡约课，但名单展示会员身份，不标记为试听生，不仅因用试听卡就创建试听转购待办。套餐保存当次名称/数量/AUD参考价快照，不代表系统核验到账。

学生列表、老师名单、试听人数和反馈必填规则按Student.type判断；新生时间标签使用firstPurchasedAt。名单和每条教学记录返回一致的membershipCategory及兼容category，按课次的墨尔本日期计算；学生页/详情顶部按今天分类，因此未来课次可能已显示会员而当前仍为新会员。会员查看首次购课日前的历史课次仍显示会员，购课当日按日历日算新会员。历史快照仅作为事件记录，不覆盖当前会员身份；Teacher不取得首次购买时间或课时余额，其他Admin不取得个体教学反馈。旧换课/恢复/重约关联属于历史已实现能力，本期收起入口及公开命令，不继续扩展；历史记录保留。

## 4. 必须由服务端执行的规则

下表是最新目标规则，必须由服务端执行；01g只完成数据契约，后续命令未实现前不能声明整条流程已通过。

| 强制规则 | 执行层 |
| --- | --- |
| Admin看全局基础资料，只管理本人学生；Teacher只见本人课次及教学资料；Task仅受派人可见 | 认证Guard、Service对象授权及响应字段白名单；联系人/销售资料仅学生负责人可见 |
| 录入数据有值须格式正确；负责人和录入人来自登录会话 | DTO与Service；联系人完整性不作为本期预约门槛 |
| 学生、老师时间不重叠，相邻课允许；不做班级、容量、级别和科目资格限制 | Service事务内检查半开区间和有效名单，短写锁处理并发 |
| 预约必须有对应卡的可用课时；同课次不得重复加入，不限制同科目累计上课次数 | 事务内余额与占用校验、参与记录唯一约束 |
| 正式卡首次发放将type改为MEMBER；试听卡不改变身份 | 发卡与身份/首次时间同事务，数据库一致性约束 |
| 仅改未来课次；已有名单可改科目/老师/时间，改课影响全体；取消课程保留历史并释放占用 | 状态/版本校验，变更记录及逐学生隐藏任务同步在同一事务 |
| 时间到不自动算到场；当前老师逐人签到，已签到且下课才允许个人评价 | 签到与唯一扣课同事务；个人评价与Admin待办同事务，二者分开 |
| 重试不重复预约或建任务，旧页面不能覆盖新结果 | 幂等收据、来源唯一索引、反馈内容校验及版本冲突 |
| 本次跟进完成统一DONE；有意向/考虑中/不报名/未联系上是结果，不能直接授予会员 | 沟通与结果/任务同事务；购课结果要求真实发卡凭证，旧完成结果不覆盖 |

## 5. 页面、首页与操作路径

以PC为主要使用环境：左侧208px菜单常驻，不按窗口断点收起；右侧为52px顶部栏和独立滚动的业务区。顶部显示当前用户、角色、退出及个人待办通知；通知查询现有本人OPEN待办、每30秒刷新，点击查看前5项或进入全部待办。正文13–14px、页面标题20px，表格与表单统一紧凑间距。极窄窗口保留桌面结构并允许横向滚动，不通过整体缩放或devicePixelRatio反向补偿字体。

左菜单为我的待办、课表、学生。Admin登录先看本人待跟进学生，按到期时间排序，可筛逾期；Teacher先看本人已签到且已下课试听学生的待评价项；从本人课表进入点名。课表是Admin全班/Teacher本人两种权限视图，展示时间、班级、科目、老师、人数及试听标签。核心草图见附录；权益开发版已增加学生三Tab及Admin课时管理入口，账户管理入口待后续实现。

以下从登录后首页计算“操作阶段”，一阶段可含选择或填写，不把每个输入框算一次点击；前提是已有学生与课次。

| 高频操作 | 路径及阶段数 |
| --- | --- |
| Admin预约试听 | ①进入课表 ②筛选并打开课次 ③在详情内选本人学生及使用的课时卡 ④点击Add；4阶段 |
| Teacher点名 | ①打开本人课程 ②打开点名弹窗 ③逐人点击签到；服务端成功后显示对勾，无课程结束按钮 |
| Teacher个人评价 | ①打开已签到且已下课学生的待办 ②填写该生评价 ③提交；3阶段，其他学生独立完成 |
| Admin跟进 | ①打开待办 ②阅读反馈并按需生成建议、实际联系家长 ③录家长反馈/顾虑/核心问题及结果，完成本次跟进；3阶段，联系在系统外完成 |
| Admin建档 | ①进入学生页 ②点新增 ③填写并保存；3阶段，联系人可后补 |

名单选人保留双池简洁提示及余额不足补课返回；只提供Add/Remove。Teacher从课程“点名”弹窗逐人签到，成功显示“✓ 已签到”，失败保持未签到并可重试。每个试听学生加入课程时预建隐藏TRIAL_FEEDBACK，签到且now>=endsAt才进入个人待办和通知数；3名2签到即返回2条，无cron或延时队列。修改老师/时间同步隐藏任务，取消课程/移除预约取消对应任务。

评价完成后，仍是试听身份的学生产生负责Admin的跟进；Admin记录家长反馈、顾虑、核心问题及原因标签（价格、时间、课程适配、教学体验、孩子意愿、家庭安排、其他）。任务status为DONE，followupOutcome分别为INTERESTED、CONSIDERING、NOT_INTERESTED、UNREACHABLE；PURCHASE_RECORDED只能由正式发卡核验。未联系上只记录联系尝试，不编造反馈；不报名不等于待办没完成。以后购课不覆盖旧沟通结果，本期不自动续建/重开任务。

上述流程目前只完成01g数据契约；页面/命令按计划逐步替换旧实现。页面保留加载、空、错误状态，失败不丢输入，不用固定说明横幅代替清晰字段。

## 6. AI与技术选择

千问只在Admin跟进详情提供结构化摘要、带来源的观察、待确认问题和沟通提纲/草稿。输入限当前学生/科目反馈及相关沟通，去除已知姓名与联系方式；自由文本仍应避免敏感信息。服务端校验JSON字段、长度、渠道和引用ID；10秒超时或校验失败返回明确标记的同语言模板，不输出成交概率、不自动发送、不改待办。真实模型成功调用仍须验证，模板不能替代。

React/TypeScript、MUI和React Query组织表单与查询；NestJS/Prisma连接PostgreSQL，OpenAPI统一契约，pnpm的common包共享类型。Compose开发挂载源码热更新；生产镜像内构建，由前端Nginx代理API，仅开放前端端口。启动与验证见[README](README.md)。

## 7. 假设、待确认与交付边界

假设每生仅一位负责Admin及一个主要联系人；本次不开放通用学生转交；账号停用流程已允许超级管理员执行必要的全量交接。时间统一Australia/Melbourne，周一至周日可排课，约60节是业务规模而非上限；不存在或有双解的当地时间拒绝输入。未来课时池统一按节、缺席不扣，需业务方确认。

最想确认：①学生转交或Admin代班怎样审批？②不同科目/时长是否扣不同单位？③正式缺席是否扣课？④误登记、退款和冲正怎样审批？⑤周末跟进是否办公？未确认前按上述假设执行，不擅自扩展收费规则。

本次需用真实数据库演示预约、反馈、待办与沟通，并直接调用API验证越权、冲突、重试和回滚。seed包含多Admin学生、周课表和历史待反馈课，避免现场等待下课；模型成功与降级分别验证。公共仓库/开始邮件、git init到最后commit不超过10小时、保留历史不squash、面试前至少1小时交付须如实记录，文档不替代证据。执行检查见[提交验收](.plan/12-submission.md)。按当前安排，正文页数和PDF排版留到最后处理。

## 8. 账号管理（本轮恢复并完成）

只有超级管理员显示“系统管理 → 账号管理”，并能查看、新增、修改和停用Admin/Teacher。角色仍只有这两种；超级管理员是Admin账号的额外`isSuperAdmin`权限，普通账号不能通过表单赋值。超级权限只扩展员工管理，学生私密资料及个人待办继续遵循原有归属规则。服务器必须独立鉴权，不能仅隐藏菜单。

账号列表按姓名/邮箱、角色和状态筛选；新建填写姓名、邮箱、角色和初始密码；编辑姓名/邮箱，角色创建后只读，避免改变既有教学与学生关系。密码重置单独操作并撤销旧会话。首个超级管理员通过受控CLI和环境变量初始化，不提供默认密码，不自动升级既有普通员工，本期无角色管理和权限配置页。

“注销”指停用，保存历史而非删除账号：禁止再次登录、使已有会话失效、记录原因和审计。停用Admin若仍有学生，须原子交接全部学生和未完成跟进给另一活动Admin；停用Teacher若仍有未来/进行中课程、已签到待评价或旧整课待反馈，先替课或完成反馈；已结束未签到的隐藏评价不构成待完成教学，停用时取消。不能注销超级管理员或自己；不提供邮箱复用或重新启用。状态变更采用版本、幂等及事务保护；历史反馈和课时操作者不改写。

开发按[账号管理计划](.plan/13-accounts.md)拆成8项后端、4项前端和2项验收，每项规定接口、源码入口、边界、前置及检查。这些任务已在隔离PG与Chrome完成验收，见[账号验收](docs/ACCOUNTS-MILESTONE.md)。当前预览库已通过CLI创建super，账号页面可运行；其他环境需执行新迁移并显式初始化。

## 附录：核心页面草图（不计正文页数）

```text
A. Admin首页与跟进详情（本次切片）
左菜单                右侧主内容：我的待办
我的待办              [待处理/已完成] [搜索] [仅逾期]
课表                  学生 / 来源课程 / 老师 / 到期 / [打开]
学生                  打开后：出勤 → 老师反馈 → 历次沟通
                      [生成建议] → 编辑或复制提纲
                      实际沟通 / 顾虑 / 核心问题 / 结果 [完成本次跟进]

B. 课表与名单（本次切片）
[周日历/列表] [关键词] [班级] [科目] [老师] [+课次]
周一至周日：时间 / 班级 / 科目 / 老师 / 学生人数
点课次 → 详情与名单：试听生置顶，显示新生/老生标签
Admin：[选本人学生] [所用课时卡：试听卡/正式卡] [Add]
       [移除学生] [编辑科目/时间/老师] [取消课程] [修改记录]
Teacher：只读本人课次和名单，[点名] → 逐人[签到] → ✓ 已签到

C. Teacher首页与反馈（本次切片）
左菜单                右侧主内容：我的待办
我的待办              [待处理/已完成] 学生 / 课程 / 结束时间 / [评价]
我的课表              打开后：学生 / 试听等标签 / 出勤 / 个人反馈
我的学生              Ava   试听    到课    [必填反馈]
                      Leo   新生    未到    [可选]
                      [课堂总结（可选）] [提交全班反馈]

D. 学生与课时（页面已实现；隔离数据库验证）
[试听学生] [新会员] [会员学生]                 [+新增学生]
新增学生：[姓名/年级] [家长姓名/关系/工作/年龄/性别/邮箱/手机]
          [沟通偏好/意向科目与时间/学习目标] [✓ 赠送试听课时1节]
          录入Admin与负责人由会话自动确定 [保存]
课时管理：[学生搜索] 试听余额/占用/可用 正式余额/占用/可用
[新增课时] → 选学生 → 选试听/正式
  试听：输入节数；正式：自定义节数 / 选择预设会员套餐
  [备注] [确认新增]；流水只读，套餐数量和参考价保留快照
```
