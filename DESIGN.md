# 学生管理系统：试听预约与课后跟进

**开发数据约定（2026-09-17）：**当前未发布，用户已明确不需要历史数据兼容，本项目开发库可直接删除重建并重新seed。后续移除旧字段和流程时，不增加历史回填、双版本或旧收据隔离；新业务自身的流水、审计和幂等仍保留。下文旧阶段验收只是当时事实。

## 1. 业务理解与本次切片

顾问最需要知道“学生下一步该约课还是联系”，老师最需要知道“今天谁来、谁要重点观察”。因此围绕一次试听连接排课、教学反馈和顾问行动，而不是把每张表都做成管理页面。仅Admin和Teacher登录；家长是沟通对象，学生是学习对象，二者信息分别保存。

**本次Part B：学生建档 → 加入课程 → 老师签到 → 下课后逐试听学生评价 → 负责Admin的待办 → AI辅助 → 记录沟通结果并完成本次跟进。** 选择它，是因为它同时覆盖排课冲突、老师识别试听生和试听后漏跟进三个痛点，能在6–10小时预算内验证跨角色、跨前后端的完整流程。

本次交付终点是有据可查的跟进结果，未报名也可以完成任务。2026-09-18按用户要求集中完成非AI部分：教师逐人评价表单、Admin四种结果统一完成、沟通顾虑与原因标签、旧流程清退及演示数据。当前非AI代码已接通并通过本轮数据库、浏览器及独立Compose验收；完整证据以[本轮验收记录](docs/NON-AI-COMPLETION.md)为准，不以编译成功代替浏览器/数据库验证。

本轮明确排除AI对接：08a上下文、08b建议页面和12c真实千问验收继续待办，跟进页面先使用教师评价和实际沟通记录。不恢复历史数据导入、生产切换、套餐管理或其他已经退出本期的功能。旧整课反馈、专用转课/恢复/重开及每科一次试听接口已移除；取消后重新加入走普通Add。

## 2. 范围与取舍

| 本次做 | 为什么 |
| --- | --- |
| 学生及主要联系人、咨询备注、沟通记录 | 同一处保存咨询和跟进依据，避免另建CRM |
| 全班课表、单次排课、名单调整与变更记录 | 顾问能直接寻找空位、避免冲突，老师看到实际名单 |
| 试听优先标签、出勤反馈、两类个人待办 | 让教学观察交到负责顾问手中，明确下一步责任人 |
| AI建议（本轮排除，后续接入） | 当前人工跟进可独立完成，未来减少阅读和组织话术时间 |

**有意后做：**已完成课时发放和套餐选择继续复用，但套餐管理、动态优惠和续费提醒不再扩展；固定周课及长期入班会增加周期和调课例外，先验证单次预约；支付、退款涉及收款核对及审批，本次只设计追加流水的扩展方向。家长资料内嵌Student，不建独立家长管理，接受兄弟姐妹重复录入以减少操作；动态角色/菜单配置、家长端与自动发消息不影响试听闭环，暂不做。班级、科目仍以seed维护；员工管理已接入super账号模块。旧数据核对导入及生产切换演练延期。专用换课/恢复/重约流程退出本期；单个学生换课由Admin先移除再添加，不承诺跨两步自动回滚。

## 3. 模型、关系与课时

| 实体 | 关键字段与关系 |
| --- | --- |
| User / Student / StudentAdminLink | 用户角色；学生name、yearLevel、type（TRIAL/MEMBER）及主要联系人。关联表保存studentId、当前adminId、最初createdByAdminId与创建时间；一生一个当前负责人，一个Admin可负责多名学生。联系方式不唯一 |
| ClassGroup / Course / ClassSession | 长期班级 / 科目 / 单次课。课次关联班级、科目、老师、起止时间和状态；班级仅分组，不保存容量限制字段 |
| SessionParticipant | sessionId+studentId唯一；kind仅记录所用课时卡TRIAL/REGULAR；记录预约状态、出勤、checkedInAt/checkedInBy、个人反馈及feedbackSubmittedAt，不表示课程或学生类型；学生可参加不同班级的课次 |
| Task / CommunicationLog | 老师每个试听参与记录一个TRIAL_FEEDBACK；Admin每个已评价且仍为试听身份的参与记录一个跟进任务。Task.status和followupOutcome分开；沟通只追加，保存家长反馈、concerns、coreQuestion、reasonTags |
| ScheduleChange | 排课或名单操作的原因、操作者、时间、前后值，与业务修改同时保存 |
| EntitlementEntry / LessonPackage | 不可变课时流水 / 预设正式课时套餐；赠课、追加、购买、签到消费的来源可追溯，套餐保存当次快照 |

**学生基本资料：**独立保存学生性别gender与年龄age，不混用家长字段。新建/编辑均可录入，年龄为0–120整数并允许清空，未知性别可留空；不从姓名、年级推算。年龄表示人工登记值，不按年份自动增长。学生列表显示姓名、年级、学生性别、学生年龄；Admin额外看到当前负责Admin姓名（取StudentAdminLink），不是原始录入人。Teacher只看到本人教学范围学生的基本信息，不增加负责人/录入人或家长资料访问权限。

**建档与家长资料：**新增学生时可直接录入家长姓名、与学生关系、工作、年龄、性别、邮箱、手机/电话、微信、沟通方式及语言，同时填写意向科目、时间和学习目标；编辑与详情保持相同字段。尚未问清的信息允许留空，年龄为可清空的0–120整数、性别可不提供，不推断家庭属性；邮箱/电话有值时必须合法；联系人不完整暂不阻止预约，由Admin补齐以便沟通。家长资料内嵌学生，兄弟姐妹可重复，不做联系人去重或独立家长管理页。这些字段仅本人负责Admin可见，不进入Teacher响应，也不因增加字段而自动发送给AI。

**学生与Admin关联：**后端从登录会话确定录入人，不接受客户端传入负责人、录入人或嵌套关联。建档、StudentAdminLink、赠课和收据同事务；新记录的当前负责人和录入人都为当前Admin。首次录入人一经记录不可修改，后续交接仅改变负责人；Admin详情显示双方姓名，Teacher不返回员工关联字段。历史记录只回填现有负责人，录入人未知保留NULL。当前兼容阶段的唯一归属写入口仍是Student.ownerAdminId，数据库触发器同步关联表，并通过唯一/外键约束保证每生恰好一条匹配关联；不能直接单独修改关联表造成权限不一致。待整体切换时再退役兼容字段，不在本次同时重写全部预约/跟进消费者。

**课表是ClassSession的日历/列表查询，没有课表实体。所有课次都是正常课程，不分“试听课程”和“正式课程”。** 同一课次可以有试听生、新会员及会员；预约kind只指定使用的课时卡，不能用于判断学生身份。

**学生身份：**Student.type是身份依据，枚举TRIAL（试听学生）与MEMBER（正式会员）。所有新建学生默认TRIAL，包括未勾选赠卡的学生。首次录入任何非试听课时卡（自定义正式卡或会员套餐）时，在发卡同一事务内将type设为MEMBER并记录firstPurchasedAt。后续发试听卡、使用剩余试听课时、续购或余额归零，都不改变会员身份。资料编辑接口不能直接修改type或首次购买时间。三Tab：type=TRIAL进入试听学生；type=MEMBER再按首次购买的墨尔本日期分为7日内新会员、之后会员学生。“新会员”是会员的时间分类，不是第三个持久化type。

**课时卡与流水（建档、分类和发放页面及签到消费后端已实现）：**新建学生默认勾选赠送试听卡，选中则在现有课时表EntitlementEntry创建TRIAL/INITIAL_TRIAL、quantity=1的记录；未选中不写记录。课时管理追加试听卡节数，或录入正式卡节数/选择正式会员套餐；不另建重复的卡余额表。两类卡跨科目通用、不可互抵，预约明确选择所用卡且每次占1节。新设计在老师签到时扣1，评价不再扣课；取消或未签到到下课后不再占用。占用查询限定未签到且endsAt大于同次服务端now，课后补签须重新校验余额；06b已实现，05d已接入操作页面。会员也可以用试听卡约课，但名单展示会员身份，不标记为试听生，不仅因用试听卡就创建试听转购待办。套餐保存当次名称/数量/AUD参考价快照，不代表系统核验到账。

学生列表、老师名单、试听人数和反馈必填规则按Student.type判断；新生时间标签使用firstPurchasedAt。名单和每条教学记录返回一致的membershipCategory及兼容category，按课次的墨尔本日期计算；学生页/详情顶部按今天分类，因此未来课次可能已显示会员而当前仍为新会员。会员查看首次购课日前的历史课次仍显示会员，购课当日按日历日算新会员。历史快照仅作为事件记录，不覆盖当前会员身份；Teacher不取得首次购买时间或课时余额，其他Admin不取得个体教学反馈。旧换课/恢复/重约属于已退役功能，当前接口、模型和页面不保留兼容分支；旧实现可从Git历史追溯。

## 4. 必须由服务端执行的规则

下表规则由服务端执行；实际验收及未验证项见本轮记录。

| 强制规则 | 执行层 |
| --- | --- |
| Admin看全局基础资料，只管理本人学生；Teacher只见本人课次及教学资料；Task仅受派人可见 | 认证Guard、Service对象授权及响应字段白名单；联系人/销售资料仅学生负责人可见 |
| 录入数据有值须格式正确；负责人和录入人来自登录会话 | DTO与Service；联系人完整性不作为本期预约门槛 |
| 学生、老师时间不重叠，相邻课允许；不做班级、容量、级别和科目资格限制 | Service事务内检查半开区间和有效名单，短写锁处理并发 |
| 预约必须有对应卡的可用课时；同课次不得重复加入，不限制同科目累计上课次数 | 事务内余额与占用校验、参与记录唯一约束 |
| 正式卡首次发放将type改为MEMBER；试听卡不改变身份 | 发卡与身份/首次时间同事务，数据库一致性约束 |
| 仅改未来课次；已有名单可改科目/老师/时间，改课影响全体；取消课程保留历史并释放占用 | 状态/版本校验，变更记录及逐学生隐藏任务同步在同一事务 |
| 时间到不自动算到场；当前老师逐人签到，已签到且下课才允许个人评价 | 签到与唯一扣课同事务；个人评价与Admin待办同事务，二者分开 |
| 重试不重复预约或建任务，旧页面不能覆盖新结果 | 幂等收据、来源唯一索引、反馈内容校验及版本冲突 |
| 本次跟进完成统一DONE；有意向/考虑中/不报名/未联系上是结果，不能直接授予会员 | 沟通与结果/任务同事务；购课结果要求真实发卡凭证，旧完成结果不覆盖 |

## 5. 页面、首页与操作路径

以PC为主要使用环境：左侧208px菜单常驻，不按窗口断点收起；右侧为52px顶部栏和独立滚动的业务区。顶部显示当前用户、角色、退出及个人待办通知；通知查询现有本人OPEN待办、每30秒刷新，点击查看前5项或进入全部待办。正文13–14px、页面标题20px，表格与表单统一紧凑间距。极窄窗口保留桌面结构并允许横向滚动，不通过整体缩放或devicePixelRatio反向补偿字体。

左菜单为我的待办、课表、学生。Admin登录先看本人待跟进学生，按到期时间排序，可筛逾期；Teacher先看本人已签到且已下课试听学生的待评价项；从本人课表进入点名。课表是Admin全班/Teacher本人两种权限视图，展示时间、班级、科目、老师、人数及试听标签。核心草图见附录；权益开发版已增加学生三Tab及Admin课时管理入口，账号管理已实现，仅super可见。

以下从登录后首页计算“操作阶段”，一阶段可含选择或填写，不把每个输入框算一次点击；前提是已有学生与课次。

| 高频操作 | 路径及阶段数 |
| --- | --- |
| Admin预约试听 | ①进入课表 ②筛选并打开课次 ③在详情内选本人学生及使用的课时卡 ④点击Add；4阶段 |
| Teacher点名 | ①打开本人课程 ②打开点名弹窗 ③逐人点击签到；服务端成功后显示对勾，无课程结束按钮 |
| Teacher个人评价 | ①打开已签到且已下课学生的待办 ②填写该生评价 ③提交；3阶段，其他学生独立完成 |
| Admin跟进 | ①打开待办 ②阅读反馈并实际联系家长（AI后续接入） ③录家长反馈/顾虑/核心问题及结果，完成本次跟进；3阶段，联系在系统外完成 |
| Admin建档 | ①进入学生页 ②点新增 ③填写并保存；3阶段，联系人可后补 |

名单选人保留双池简洁提示及余额不足补课返回；只提供Add/Remove。Teacher从课程“点名”弹窗逐人签到，成功显示“✓ 已签到”，失败保持未签到并可重试。每个试听学生加入课程时预建隐藏TRIAL_FEEDBACK，签到且now>=endsAt才进入个人待办和通知数；3名2签到即返回2条，无cron或延时队列。修改老师/时间同步隐藏任务，取消课程/移除预约取消对应任务。

评价完成后，仍是试听身份的学生产生负责Admin的跟进；Admin记录家长反馈、顾虑、核心问题及原因标签（价格、时间、课程适配、教学体验、孩子意愿、家庭安排、其他）。任务status为DONE，followupOutcome分别为INTERESTED、CONSIDERING、NOT_INTERESTED、UNREACHABLE；PURCHASE_RECORDED只能由正式发卡核验。未联系上只记录联系尝试，不编造反馈；不报名不等于待办没完成。以后购课不覆盖旧沟通结果，本期不自动续建/重开任务。

上述非AI页面和命令已按本轮实现，旧接口不再对外提供。页面保留加载、空、错误状态，失败不丢输入，不用固定说明横幅代替清晰字段。

## 6. AI与技术选择

千问后续只在Admin跟进详情提供结构化摘要、带来源的观察、待确认问题和沟通提纲/草稿。输入限当前学生/科目反馈及相关沟通，去除已知姓名与联系方式；自由文本仍应避免敏感信息。服务端校验JSON字段、长度、渠道和引用ID；10秒超时或校验失败返回明确标记的同语言模板，不输出成交概率、不自动发送、不改待办。真实模型成功调用仍须验证，模板不能替代。

React/TypeScript、MUI和React Query组织表单与查询；NestJS/Prisma连接PostgreSQL，OpenAPI统一契约，pnpm的common包共享类型。Compose开发挂载源码热更新；生产镜像内构建，由前端Nginx代理API，仅开放前端端口。启动与验证见[README](README.md)。

按2026-09-18部署追加要求，新增GitHub Actions检查后发布：main自动/手动触发，runner构建amd64镜像，经SSH上传到新加坡轻量服务器。用户明确开发服务器不考虑数据库迁移：Compose仅保留web（含Nginx）/api/db，数据库镜像首次空卷初始化当前schema快照及约束/触发器，已有库不升级、不自动重建。暂以公网IP的HTTP入口演示，服务器独立生成并保留运行密钥；首次空卷自动执行建表及演示SQL，HTTPS与账号见README。工作流配置和本地验证不代表GitHub远程发布已完成。

用户最新要求删除迁移历史，首次空卷自动准备演示数据：`sql/development-seed.sql`按现有业务规则提供1个super（存在则复用）、5个普通Admin、20个Teacher、10会员与5试听学生、10正式/5试听充值、3节课程以及可见教师评价和负责Admin跟进。签到消费与充值分开；重复导入不覆盖真实操作，不改变密码或补满余额，已有卷不重复运行。

## 7. 假设、待确认与交付边界

假设每生仅一位负责Admin及一个主要联系人；本次不开放通用学生转交；账号停用流程已允许超级管理员执行必要的全量交接。时间统一Australia/Melbourne，周一至周日可排课，约60节是业务规模而非上限；不存在或有双解的当地时间拒绝输入。课时池统一按节、缺席不扣，需业务方确认。

最想确认：①学生转交或Admin代班怎样审批？②不同科目/时长是否扣不同单位？③正式缺席是否扣课？④误登记、退款和冲正怎样审批？⑤周末跟进是否办公？未确认前按上述假设执行，不擅自扩展收费规则。

本次需用真实数据库演示预约、反馈、待办与沟通，并直接调用API验证越权、冲突、重试和回滚。seed包含多Admin学生、周课表和历史待反馈课，避免现场等待下课；模型成功与降级分别验证。公共仓库/开始邮件、git init到最后commit不超过10小时、保留历史不squash、面试前至少1小时交付须如实记录，文档不替代证据。执行检查见[提交验收](.plan/12-submission.md)。按当前安排，正文页数和PDF排版留到最后处理。

## 8. 账号管理（本轮恢复并完成）

只有超级管理员显示“系统管理 → 账号管理”，并能查看、新增、修改和停用Admin/Teacher。角色仍只有这两种；超级管理员是Admin账号的额外`isSuperAdmin`权限，普通账号不能通过表单赋值。超级权限只扩展员工管理，学生私密资料及个人待办继续遵循原有归属规则。服务器必须独立鉴权，不能仅隐藏菜单。

账号列表按姓名/邮箱、角色和状态筛选；新建填写姓名、邮箱、角色和初始密码；编辑姓名/邮箱，角色创建后只读，避免改变既有教学与学生关系。密码重置单独操作并撤销旧会话。开发服务器首次空卷SQL创建演示super；其他未初始化super的环境通过受控CLI和环境变量初始化，不自动升级既有普通员工，本期无角色管理和权限配置页。

“注销”指停用，保存历史而非删除账号：禁止再次登录、使已有会话失效、记录原因和审计。停用Admin若仍有学生，须原子交接全部学生和未完成跟进给另一活动Admin；停用Teacher若仍有未来/进行中课程、已签到待评价，先替课或完成反馈；已结束未签到的隐藏评价不构成待完成教学，停用时取消。不能注销超级管理员或自己；不提供邮箱复用或重新启用。状态变更采用版本、幂等及事务保护；历史反馈和课时操作者不改写。

开发按[账号管理计划](.plan/13-accounts.md)拆成8项后端、4项前端和2项验收，每项规定接口、源码入口、边界、前置及检查。这些任务已在隔离PG与Chrome完成验收，见[账号验收](docs/ACCOUNTS-MILESTONE.md)。当前预览库已通过CLI创建super，账号页面可运行；新的Compose环境首次建表后仍需显式初始化super。

## 9. 唯一本地环境、账号与启动

2026-09-18按用户要求统一为 **[http://127.0.0.1:18080/login](http://127.0.0.1:18080/login)**。旧18084/18085等环境及预览进程已关闭，旧StudentSys镜像已清理；当前仅保留web/api/db三个`local18080`镜像。仅web发布`127.0.0.1:18080`，API和PostgreSQL通过容器内网访问。

### 9.1 SQL初始化后的账号

当前库使用[sql/development-seed.sql](sql/development-seed.sql)初始化，以下均为虚构演示账号，统一密码 **`StudentSysDemo2026!`**，不再使用之前的`@example.com`或`@preview.test`账号。

| 类型 | 登录账号 | 数量 | 推荐演示 |
| --- | --- | ---: | --- |
| 超级管理员 | `super@demo.studentsys.test` | 1 | 账号管理；并不绕过学生负责人限制 |
| Admin | `admin01@demo.studentsys.test`、`admin02@demo.studentsys.test`、`admin03@demo.studentsys.test`、`admin04@demo.studentsys.test`、`admin05@demo.studentsys.test` | 5 | admin01有1条试听跟进；每人负责3名学生 |
| Teacher | `teacher01@demo.studentsys.test`至`teacher20@demo.studentsys.test`（两位数字，连续编号） | 20 | teacher01有1条待评价；teacher02有未签到学生；teacher03有未来课程 |

数据包括15名学生（5试听、5新会员、5会员）、3节课程（墨尔本昨日2节、明日1节）、15条预约、15条正数课时记录和12条签到扣课记录。5条教师任务中1条已完成，只有1条OPEN当前可见，另3条因未签到/未下课隐藏；Admin有1条OPEN跟进。超级管理员没有负责的学生，所以自己的课时列表为空，业务演示使用admin01。

### 9.2 启动、停止及查看日志

配置保存在本机被Git忽略的`.env.local18080`，包含独立数据库凭据、账号命令密钥、`IMAGE_TAG=local18080`、`WEB_PORT=18080`、`WEB_BIND_ADDRESS=127.0.0.1`和本机HTTP的`SESSION_COOKIE_SECURE=false`。当前使用`compose.prod.yaml`的镜像运行方式，**不挂载源码、不提供热更新**。

```sh
cd /Users/Zhuanz/work/studentSys
# 先启动Docker Desktop；重新启动已有环境，不重复初始化已有卷
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml up -d --wait

# 修改代码后重新构建并启动同一个环境
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml up -d --build --wait

# 状态、日志
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml ps
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml logs -f api web

# 停止，保留数据库卷
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml down
```

当前数据库镜像在空卷首次启动时按`001-schema.sql`、`002-development-seed.sql`顺序执行建表和演示数据；已有卷启动不会重建或补数据。本轮已在新库显式执行指定SQL并核对。需要手动执行同一文件时：

```sh
docker compose --env-file .env.local18080 -p studentsys-local -f compose.prod.yaml exec -T db \
  sh -c 'exec psql -X -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < sql/development-seed.sql
```

SQL一次事务完成；重复导入依据完成标记跳过，不补余额、不重开任务。当前账号由SQL创建，不再额外运行旧Prisma演示seed。旧库备份保存在本机`tmp/consolidation-backups/`；测试写入在验收后还原到本次SQL的初始状态。详见[18080整合验收](docs/LOCAL18080-VERIFICATION.md)。

## 附录：核心页面草图（不计正文页数）

```text
A. Admin首页与跟进详情（本次切片）
左菜单                右侧主内容：我的待办
我的待办              [待处理/已完成] [搜索] [仅逾期]
课表                  学生 / 来源课程 / 老师 / 到期 / [打开]
学生                  打开后：出勤 → 老师反馈 → 历次沟通
                      实际沟通 / 顾虑 / 核心问题 / 结果 [完成本次跟进]

B. 课表与名单（本次切片）
[周日历/列表] [关键词] [班级] [科目] [老师] [+课次]
周一至周日：时间 / 班级 / 科目 / 老师 / 学生人数
点课次 → 详情与名单：试听生置顶，显示新生/老生标签
Admin：[选本人学生] [所用课时卡：试听卡/正式卡] [Add]
       [移除学生] [编辑科目/时间/老师] [取消课程] [修改记录]
Teacher：只读本人课次和名单，[点名] → 逐人[签到] → ✓ 已签到

C. Teacher首页与反馈（本次切片）
左菜单                右侧主内容：我的待办
我的待办              [待处理/已完成] 学生 / 课程 / 结束时间 / [评价]
我的课表              打开后：学生 / 试听等标签 / 出勤 / 个人反馈
我的学生              Ava   试听    到课    [必填反馈]
                      能力/偏好（可选）
                      [提交本学生评价]；其他学生独立处理

D. 学生与课时（页面已实现；隔离数据库验证）
[试听学生] [新会员] [会员学生]                 [+新增学生]
新增学生：[姓名/年级] [家长姓名/关系/工作/年龄/性别/邮箱/手机]
          [沟通偏好/意向科目与时间/学习目标] [✓ 赠送试听课时1节]
          录入Admin与负责人由会话自动确定 [保存]
课时管理：[学生搜索] 试听余额/占用/可用 正式余额/占用/可用
[新增课时] → 选学生 → 选试听/正式
  试听：输入节数；正式：自定义节数 / 选择预设会员套餐
  [备注] [确认新增]；流水只读，套餐数量和参考价保留快照
```
