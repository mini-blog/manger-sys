# 学生管理系统：试听与跟进

## 1. 业务判断与范围

**版本说明：**本文已纳入学生三类Tab与独立权益管理的新需求；既有试听切片已实现，本轮变更尚待开发，执行状态见[plan](plan/README.md)。

机构有10名Admin（各负责约100名学生）、20名Teacher，每周约60节课。首版优先解决排课冲突、老师不认识新生、试听后无人跟进。选取完整切片：**学生建档 → 预约试听 → 老师课后出勤与反馈 → 负责Admin收到待办 → 参考AI建议联系家长 → 记录沟通 → 手动登记购课、发放权益**。

学生与家长不是同一个人，但首版只在Student内嵌一位主要联系人：姓名、关系、电话、邮箱、微信、偏好渠道及语言。不建家长表或管理页；兄弟姐妹重复录入可接受，修改只影响当前学生，电话不作学生唯一标识。仅Admin和Teacher登录。

**实现范围：**学生三类Tab、学生资料与沟通记录、独立权益管理、课表日历/列表、单次排课与名单调整、试听及正式课时预约占用与到课扣减、老师反馈、两类个人待办、课次变更记录、千问跟进辅助。

**暂不做：**课时套餐及优惠定价、在线支付/收费退款、家长端、独立咨询商机、账号/菜单管理、批量周课模板、自动发送。员工、班级、科目以seed维护。实际验证与限制见README；详细规格见IMPLEMENTATION。

## 2. 数据模型与业务边界

| 实体 | 关键字段及关系 |
| --- | --- |
| User | id、name、email、passwordHash、role；Cookie会话登录 |
| Student | name、yearLevel、ownerAdminId、firstPurchasedAt?、version；内嵌联系人及学习目标/意愿时间/科目备注。新建负责人取当前Admin |
| Course / ClassGroup | 科目目录 / 固定班级和面向年级；两者不固定绑定，不单独建维护页 |
| ClassSession | 一次实际授课：班级、科目、teacherId、startsAt/endsAt、capacity、status、version、summary?、feedbackSubmittedAt? |
| SessionParticipant | sessionId+studentId唯一；kind TRIAL/REGULAR、bookingStatus、attendance、个人反馈/能力/偏好、标签快照、version；同时是预约和本课名单 |
| Task | type、assigneeId、sessionId、participantId?、来源快照、availableAt、dueAt、status、reason、purpose、version；每课一个老师任务，每试听参与记录一个Admin任务 |
| EntitlementEntry | studentId、bucket TRIAL/REGULAR、kind、带符号quantity、participantId?、actorId、createdAt；不可变课时流水，购课记录是会员依据 |
| CommunicationLog | studentId、可选taskId/participantId、联系人姓名/关系快照、渠道、内容、结果、occurredAt、createdBy；独立追加 |
| ScheduleChange | 源/目标课次、学生、参与记录、操作者、时间、原因、前后值、请求标识；与排课修改同事务 |

**课表只是ClassSession查询，没有课表实体。** 班级是长期分组，课次是具体某节课；允许同生参加不同班级的单次课，只要时间不冲突。本版不宣称已经实现正式长期入班。Admin看全局课表和学生基础数据，但只能管理本人学生；Teacher只看本人课次、名单和教学资料。联系信息、沟通和销售跟进仅学生负责人可见；所有Task仅assignee可读写，Admin也不能越权。

**学生管理固定三个互斥Tab。** 试听学生=从未登记购买正式课时；新会员=首次购课当地日期起7个墨尔本日历日内；会员学生=首次购课当地日期起已满7日。未买课但试听已用完仍在试听学生；续购不重置首次购课时间，正式余额归零不取消会员身份。分类由服务端推导，不允许手动切换。学生页按今天分类，课次名单按课次日期判断新会员，历史反馈保存标签快照。

**试听权益与会员课时独立。** 新增学生填写初始试听课时，正整数、默认1、最低1；建档同事务发放，可以未试听直接购买正式课时。最低1约束的是初始发放量，消费后可为0。首次购课与续购都不清零、不补发试听课时。TRIAL是本次参与方式，与学生是否会员无关；会员仍可试听，名单试听置顶并可带新会员/会员辅助标签。

**独立权益管理，仅负责人Admin操作本人学生。** 新增弹窗选择学生、手动输入购买课时数（正整数）、填写备注，确认后立即写入正式课时购入流水；首次成功登记同时设置firstPurchasedAt。无需先选套餐或完成试听。此操作是Admin对线下购课的人工登记，不代表系统核验到账；本期无套餐管理、优惠、价格计算或支付流程。套餐（固定课时、优惠价）留后续扩展。

本期采用**每学生两个跨科目通用课时池**，TRIAL与REGULAR不可互相抵扣；每节到课消耗所选池1单位，与课堂分钟数无关。余额=流水数量之和，占用=该池BOOKED/PENDING参与数，可用=余额−占用。预约先占用，到课反馈提交时扣减，取消/未到释放；过期待反馈仍占用。余额不足拒绝新预约，历史扣减不会重复。取消旧“每生每科目只允许一次试听”的规则，初始发放多节时可多次预约，仍检查时间与容量。详情见IMPLEMENTATION；旧数据须核对迁移，不能凭旧入学日伪造购课。

## 3. 流程与强制规则

Admin电话/线下咨询后记录沟通，从全班课表按关键词、科目、班级、老师筛选，添加本人学生。未来课次可换老师、改时间或容量；学生可取消、恢复、同科目换课。每次调课记录原因，并确认影响名单；保存不代表已通知家长。

老师任务随排课预建，availableAt=endsAt，到时查询即可显示，不依赖cron或页面开启。替课同步移交、改时同步延期、取消同步关闭。老师课后一次提交全部有效名单的出勤；试听到课必须个人反馈，正式反馈可选。事务内保存结果、完成老师任务，并为每位试听生创建负责Admin的任务：未购课为购课跟进，已购课为会员试听反馈回访；未到/取消显示重约原因。到期时间暂定来源课次结束或取消后的下一墨尔本日17:00；补录太晚可直接逾期。

Admin记录实际沟通；未联系上/考虑中/有意向保持OPEN并设未来跟进时间，暂不考虑可关闭。“有意向”或手动关闭待办不能赋予会员身份；办理购课进入权益管理。购课事务关闭该学生开放的首次购课跟进任务并关联购课流水，保留沟通历史；取消/未到的重约任务与会员回访仍需实际处理。任务关闭后仍能追加沟通；重开需原因、未来时间，会员不能重开首次购课任务。重新预约成功关闭对应开放重约任务。

| 规则 | 服务端执行方式 |
| --- | --- |
| 本人学生、本人课次、本人待办范围不可绕过 | Guard认证；Service对象授权；DTO字段白名单 |
| 试听预约前须有联系人姓名及至少一种联系方式，所选渠道匹配 | DTO格式校验、Service预约检查 |
| 学生、老师、同班有效课次不重叠；相邻允许；不超容量 | 事务内半开时间区间与BOOKED人数检查 |
| 两个课时池独立、可用量不为负；恢复也重验 | Serializable事务、统一业务写锁、发放/扣减来源唯一约束 |
| 只改未开始课次；有名单历史不得换科目/班级；同科目换课失败不丢旧位 | 状态、version和影响名单校验；变更、记录和待办同事务 |
| 时间到不自动算出勤；仅实际分配老师在结束后提交全部名单 | 注入Clock、完整名单校验；出勤/反馈/任务原子提交 |
| 重试不重复占位、消费或创建任务；不同结果不能覆盖已提交反馈 | MutationReceipt幂等键+请求hash；反馈内容hash；来源唯一索引 |
| 沟通只追加；状态变更与日志同时成功或回滚 | DTO、version、事务；失败不产生成功收据 |

当前规模采用PostgreSQL事务级advisory lock串行化短业务写操作，并对Serializable冲突最多尝试3次，优先保证规则一致；读取不加此锁，模型调用在事务外。更高并发时再拆为按学生/老师/课次排序的细粒度锁。

## 4. AI、页面、假设与验收

唯一AI入口在Admin的跟进详情。千问接收当前学生/科目的教师反馈和最多10条相关沟通；移除已知姓名、联系方式并限制输入长度，自由文本仍须人工避免夹带敏感信息。资料当作不可信数据，无模型写工具。同时接收最小会员状态与任务目的，会员试听仅辅助回访，不重复推销首次购课。输出摘要、带来源ID的观察、待确认问题、下一步、按渠道的提纲/草稿；en-AU或zh-CN。Zod严格校验字段、长度、渠道结构及引用ID，不提供成交百分比或价格承诺。10秒超时、每用户每分钟5次；无key/失败返回同语言渠道模板并明确标记。草稿可编辑复制，不自动发送、不改变待办、不算已沟通。

Admin左菜单四个入口：**我的待办**（Admin跟进/Teacher反馈）、**课表**（Admin全班/Teacher本人，日历与列表）、**学生**（试听学生/新会员/会员学生三个Tab）、**权益管理**（本人学生发放与流水）。Teacher保留前三个入口，学生三Tab仅筛本人教学范围，不显示权益流水、联系方式或销售资料。登录仅账号、密码；页面保留加载/空/错误状态，失败留输入，无开发说明条。技术栈：React、MUI、React Query、OpenAPI、NestJS、Prisma、PostgreSQL、Docker Compose、千问。

时间固定Australia/Melbourne，UI英文/en-AU；时间点存储timestamptz，首次购课按墨尔本当地日期计算7日边界，不按168小时硬算。周一至周日可排课，60节是规模非上限，不默认学校假期停课。当地时间不存在或重复时拒绝输入，冲突按实际时间点计算。

五个后续确认问题：①Admin代班或学生转交如何批准？②不同科目/时长将来是否需要不同扣课单位？③正式缺席以后是否扣课？④误登记购课的冲正及退款如何审批？⑤跟进周末是否办公？本期按通用课时、到课扣1、缺席不扣、首次购课时间由系统记录执行；退款、余额直接编辑及人工日期更正暂不开放，未来以受控审计追加冲正。

验收使用真实PG和前后端走通整个切片；检查越权、并发抢位、换课/反馈回滚、重复提交、取消恢复、未到重约、直接购课后继续试听、双池并发占用、首次购课与续购、DST与7天边界、模型校验和回退。seed含多Admin学生、周课表、历史待反馈与跟进样例，测试注入Clock而不开放改时间接口。真实千问验证取决于后端配置，不能以模板冒充完成。原题还要求设计正文≤3页（草图除外）、保留Git历史不squash、公共仓库、开始邮件、10小时内提交及面试前至少1小时交付；外部交付状态以README为准。

---

## 附录：页面草图（不计正文页数）

```text
Admin
┌────────────┬─────────────────────────────────────────┐
│ 我的待办   │ 我的待办                     待处理 / 已完成│
│ 课表       │ 学生    来源课次    老师    到期    操作    │
│ 学生       │ Ava     数学 16:00  Emma    今天   跟进    │
│ 权益管理   │ ┌ 跟进详情 ───────────────────────────┐ │
│            │ │ 到课 / 老师反馈 / 历次沟通            │ │
│            │ │ [生成沟通建议]  摘要、依据、电话提纲  │ │
│            │ │ 结果 / 沟通记录 / 下次行动  [保存]    │ │
└────────────┴─────────────────────────────────────────┘

学生管理
[试听学生] [新会员 · 首次购课7日内] [会员学生]     [+ 新增学生]
[搜索学生]  姓名 / 年级 / 类型 / 负责人           [查看]
新增学生：姓名、年级、主要联系人、初始试听课时 [1]（最低1）

权益管理（Admin本人学生）                    [+ 登记购课]
[搜索学生]  试听余额/占用/可用  正式余额/占用/可用  [流水]
新增弹窗：学生 [选择]  购买课时 [正整数]  备注 [可选]
          [取消] [确认发放]；无套餐、价格或支付选项

课表（没有独立课表实体）
[关键词] [全部班级] [科目] [老师]  [上一周] [本周] [下一周]
[日历 / 列表]                                  [+ 课次]
日期时间       班级       科目   老师    学生/容量
周日 10–11     Year 5 A   数学   Emma       8/10
点课次 → 名单 [添加学生] [换课] [替课/改时间] [修改记录]

Teacher
┌────────────┬─────────────────────────────────────────┐
│ 我的待办   │ 待反馈 · 数学 · Year 5 A · 周日10–11     │
│ 我的课表   │ 姓名       标签        出勤      个人反馈│
│ 我的学生   │ Ava        试听        到课      ……     │
│            │ Leo        新会员        到课      ……     │
│            │ Mia        会员        未到             │
│            │ 课堂总结（可选）               [提交]   │
└────────────┴─────────────────────────────────────────┘
```
