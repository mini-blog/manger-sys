# 12c · 真实千问成功与降级验证

- 类型：验收；范围：当前试听切片；状态：待执行（本轮仅修订计划）。
- 前置任务：无；从已存在的代码开始。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[DESIGN](../../DESIGN.md)的第6节。无需通读其他任务正文。

## 唯一目标

只验证真实provider及失败时人工流程可用。

## 修改入口

packages/api/test/qwen-smoke.mjs、ai.test.ts；packages/api/src/workflow/ai.service.ts。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

后端QWEN_*由用户环境提供；读取配置是否存在即可，不输出key或真实学生资料。

## 实现边界

用虚构输入运行pnpm test:qwen，成功需source=llm、结构/引用校验通过；再检查无key/超时/非法JSON/假引用模板及人工表单。模板mock可用于失败路径但不能当成功provider证据；缺key标待配置，只此任务受阻，不阻塞无关设计/开发。

## 不在本任务中

不自购密钥、不改会员业务、不把未调用标成成功。

## 完成检查

区分真实成功与降级证据，token/key不入日志；任务范围不包括对外发送草稿。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务不依赖权益版；不把当前旧规则验收当作未来权益功能验收。

## 执行记录

尚未执行；没有代码或测试通过声明。
