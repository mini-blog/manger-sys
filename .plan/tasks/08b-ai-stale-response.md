# 08b · 过时AI建议的前端丢弃

- 类型：开发；范围：后续权益版本；状态：待执行（本轮仅修订计划）。
- 前置任务：[08a](08a-ai-context.md)、[07d](07d-followup-ui.md)。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[实现细则](../11-implementation-details.md)的11第5、7节。无需通读其他任务正文。

## 唯一目标

只避免旧任务/旧会员身份生成的建议覆盖新状态。

## 修改入口

packages/web/src/pages/TaskDetail.tsx(AiSuggestions)。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

响应studentVersion/taskVersion与最新已授权详情比较；切任务/渠道/语言递增请求序号。

## 实现边界

返回后主动刷新当前task详情再比对版本，不能只与调用开始的缓存比对；不一致丢弃草稿并允许重生成。购课后的query失效/版本变化清旧草稿；未收到推送前不承诺实时感知他人更改，恢复前台和使用草稿前复核。请求失败仍可填人工沟通，复制不视为发送或完成。

## 不在本任务中

不改provider、不加WebSocket或自动发送。

## 完成检查

慢响应后切任务/渠道、生成中购课、旧销售任务已变会员、错误/模板、不串响应；人工记录保持可用。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务完成不代表整版权益功能可上线，发布依赖见[09c](09c-cutover.md)。

## 执行记录

尚未执行；没有代码或测试通过声明。
