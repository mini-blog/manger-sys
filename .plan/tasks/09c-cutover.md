# 09c · 权益版整体切换演练

- 类型：集成；范围：后续权益版本；状态：待执行（本轮仅修订计划）。
- 前置任务：[01e](01e-migration-import.md)、[01f](01f-demo-seed.md)、[09a](09a-concurrency-verification.md)、[09b](09b-workflow-ui-verification.md)、[09d](09d-legacy-cleanup.md)。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[共同约定](../00-contracts.md)的版本上线约定。无需通读其他任务正文。

## 唯一目标

只演练并记录权益版数据与前后端整体切换。

## 修改入口

迁移运行手册；新增收紧约束migration；README.md与任务状态。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

已通过隔离数据验收、可执行迁移脚本、对应前后端构建产物；输出切换运行手册与证据。

## 实现边界

在隔离旧库副本：停写→备份→兼容扩展迁移→核对导入→加Admin purpose等收紧约束→匹配API/web整体启动→冒烟。准备两个明确迁移批次：A只含兼容扩展迁移，B再加入收紧migration。维护窗口先用A执行migrate与导入，成功后才用B的最终镜像执行收紧迁移及启动；禁止首次旧库升级就对含B的全部迁移直接compose up。失败保持维护状态，新消费发生后不得只回滚镜像到旧规则。更新DESIGN/README范围与实际验收，保留失败证据。

## 不在本任务中

不重写已完成领域服务、不自动在真实生产执行、不改Git时间、不处理PDF。

## 完成检查

一次完整旧库副本切换与失败退出；最终全库check/test/test:integration/build/OpenAPI一致，核对账与任务不变量；只清自建验证资源。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务完成只证明记录中的隔离演练结果，不表示已经部署真实环境。

## 执行记录

尚未执行；没有代码或测试通过声明。
