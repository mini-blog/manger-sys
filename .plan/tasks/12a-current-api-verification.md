# 12a · 当前试听切片API回归

- 类型：验收；范围：当前试听切片；状态：待执行（本轮仅修订计划）。
- 前置任务：无；从已存在的代码开始。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[DESIGN](../../DESIGN.md)的第1、4节。无需通读其他任务正文。

## 唯一目标

只验当前旧版代码是否符合本次Part B规则。

## 修改入口

packages/api/test/integration.mjs、workflow.mjs；docs中新增实际验收记录。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

原版单科资格，不调用尚未实现的权益接口；隔离测试PG和虚构数据。

## 实现边界

运行现有API集成测试并核对越权/并发容量/师生班级冲突/取消恢复/换课回滚/提前或不完整反馈/重复提交/沟通状态规则。演示终点为沟通，不把ENROLLED或人工日期当购买。当前代码与设计有缺陷则定位具体用例修最小范围，不引入权益功能。

## 不在本任务中

不做新会员/双池开发、UI或PDF。

## 完成检查

记录版本、命令、结果和未覆盖项；原数据库无变化；失败不可记通过。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务不依赖权益版；不把当前旧规则验收当作未来权益功能验收。

## 执行记录

尚未执行；没有代码或测试通过声明。
