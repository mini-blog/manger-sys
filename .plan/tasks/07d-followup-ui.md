# 07d · Admin跟进页的服务目的与购课入口

- 类型：开发；范围：后续权益版本；状态：待执行（本轮仅修订计划）。
- 前置任务：[07b](07b-followup-command.md)、[07c](07c-task-read.md)、[10i](10i-grant-dialog.md)。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[实现细则](../11-implementation-details.md)的11第5、7节。无需通读其他任务正文。

## 唯一目标

只改跟进展示/结果选项和下一步入口。

## 修改入口

packages/web/src/pages/MyTasks.tsx、TaskDetail.tsx(Admin跟进部分)。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

展示purpose、reason、当前会员类别、来源快照；FIRST_PURCHASE入口预选学生/REGULAR/CUSTOM。REBOOKING入口指课表并带来源taskId。

## 实现边界

移除新写ENROLLED/首次入学日期控件；RESOLVED仅回访/重约，历史已报名结果可读。购课自动关闭显示“已登记购课”，不显示到账成功。复用沟通表单，未联系不预选成功，409不覆盖服务器；旧销售任务学生已会员不再显示首次购买推销按钮。

## 不在本任务中

不实现AI异步失效处理或重新设计首页。

## 完成检查

首次购课/回访/重约三种任务、续购不误跳、历史结果可读、人工跟进与购课冲突、各Admin只本人。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务完成不代表整版权益功能可上线，发布依赖见[09c](09c-cutover.md)。

## 执行记录

尚未执行；没有代码或测试通过声明。
