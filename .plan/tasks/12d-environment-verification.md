# 12d · 当前开发与生产环境验证

- 类型：验收；范围：当前试听切片；状态：待执行（本轮仅修订计划）。
- 前置任务：[12a](12a-current-api-verification.md)、[12b](12b-current-ui-verification.md)。
- 最小阅读：[共同约定](../00-contracts.md)、本文；按需查看[DESIGN](../../DESIGN.md)的第6–7节。无需通读其他任务正文。

## 唯一目标

只验证已有Compose运行方式及隔离。

## 修改入口

compose.dev.yaml、compose.prod.yaml、README.md、scripts/check-compose.mjs；docs验收记录。路径以仓库根目录为基准；标注“新增”的文件尚不存在。沿用现有服务，不为本任务重搭模块。

## 输入与交付契约

现有镜像/Compose配置，隔离项目名和端口，禁止占用或删除用户服务/卷。

## 实现边界

运行配置检查，按修改范围验证数据库→迁移→API→web；开发热更新、生产API/DB无宿主端口、Nginx代理与登录。使用独立项目/端口/卷，不能覆盖已有开发环境；原环境继续可用。

## 不在本任务中

不核实公共仓库/邮件，不发邮件或部署，不做PDF。

## 完成检查

记录命令/退出码、正常与故障启动结果，清理本次自建资源；真实部署不是本地验证前提。

在本文件记录实际改动、检查命令/结果、未验证项，再更新总索引状态。新增API同步Swagger/OpenAPI及common生成类型；测试随本任务实现，不全部推迟到09。本任务不依赖权益版；不把当前旧规则验收当作未来权益功能验收。

## 执行记录

尚未执行；没有代码或测试通过声明。
